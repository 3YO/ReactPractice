import React, {Fragment} from 'react';

const footer = (props) => (
    <footer className="footer">
        <div className="">
            <span>Footer Test</span>
        </div>
    </footer>

);

export default footer;