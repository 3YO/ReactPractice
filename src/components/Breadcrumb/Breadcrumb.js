import React from 'react';

const breadcrumb = (props) => (
    < div className = "col-12 bg-light" >
        <div className="">
                <span> breadcrumb path here</span>
                < button className = "btn btn-dark float-right" > +New App </button>
        </div>
    </div>
);

export default breadcrumb;